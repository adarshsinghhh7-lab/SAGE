# Generic Node-host process definition (Heroku, Fly.io, Railway, a VPS, ...).
# The Express Sealing Server also serves the built React app when
# SERVE_STATIC=true is set, so UI and /api share one origin.
web: node backend/src/server.js